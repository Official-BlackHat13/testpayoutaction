-- DELIMITER &&
-- create PROCEDURE getAmount(in type varchar(20),in clientCode varchar(60),out value int)
-- begin
-- select sum(amount) into value from apis_ledgermodel where trans_amount_type=type and client_code=clientCode and status="Success";
-- end 
-- && DELIMITER ;


-- DELIMITER &&
-- create PROCEDURE getBalance(in clientCode varchar(60),out balance int)
-- begin
-- Call getAmount("credited",clientCode,@cred);
-- Call getAmount("debited",clientCode,@deb);
-- select @cred-@deb into balance;
-- end 
-- && DELIMITER ;

--DELIMITER $$
--CREATE DEFINER=`root`@`localhost` PROCEDURE `fet`(IN n varchar(1000), m int(11))
--BEGIN
--select * from apis_ledgermodel where (customer_ref_no = n OR trans_amount_type = n OR client_code = n) AND (merchant = m) ;
--END$$
--DELIMITER ;
